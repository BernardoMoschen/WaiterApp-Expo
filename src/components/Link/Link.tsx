import { Link } from "@react-navigation/native";
import styled from "styled-components/native";

export const SysLink = styled(Link)`
    color: #123038;
    font-size: 16px;
    font-weight: 500;
    text-transform: capitalize;
`;
